import { SafeAreaView, Text, View } from "react-native";
SafeAreaView

const Addresses = ({}) => {
  return (
    <SafeAreaView>
    <View><Text>coioehoi</Text></View>
    </SafeAreaView>
  );
};

export default Addresses;


